# opencode

OpenCode with Kimi K2.5 (Moonshot AI). Comment `/opencode` or `/oc` on issues or PRs to run.

## Setup

1. Add `MOONSHOT_API_KEY` to repo Secrets (Settings → Secrets and variables → Actions)
2. Install the [OpenCode GitHub App](https://github.com/apps/opencode-agent) on this repo
